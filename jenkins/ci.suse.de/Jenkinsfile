pipeline {
    agent any
    stages {
        stage('Preparation') {
            steps {
                sh "scripts/crowbar-testbuild.rb -o ${ghorg} -r ${ghrepo} -p ${pr_str} -c scripts/ardana-testbuild.yaml"
            }
        }
        stage('Build') {
            steps {
                echo "Building"
            }
        }
        stage('Results') {
            steps {
                echo "Done"
            }
        }
    }
}
